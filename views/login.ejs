<!-- views/login.ejs -->
<!DOCTYPE html>
<html lang="de">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Anmelden - Firebase MFA Demo</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/css/styles.css">
</head>
<body>
  <div class="container mt-5">
    <div class="row justify-content-center">
      <div class="col-md-6">
        <div class="card">
          <div class="card-header">
            <h3 class="text-center">Anmelden</h3>
          </div>
          <div class="card-body">
            <form id="loginForm">
              <div class="mb-3">
                <label for="email" class="form-label">E-Mail:</label>
                <input type="email" class="form-control" id="email" required>
              </div>
              <div class="mb-3">
                <label for="password" class="form-label">Passwort:</label>
                <input type="password" class="form-control" id="password" required>
              </div>
              <div class="d-grid">
                <button type="submit" class="btn btn-primary">Anmelden</button>
              </div>
            </form>
            
            <!-- MFA Eingabefeld (wird nur angezeigt, wenn MFA erforderlich ist) -->
            <div id="mfaVerification" class="mt-3 d-none">
              <div class="mb-3">
                <label for="verificationCode" class="form-label">SMS-Bestätigungscode:</label>
                <input type="text" class="form-control" id="verificationCode">
              </div>
              <div class="d-grid">
                <button id="verifyMfaBtn" class="btn btn-success">Bestätigen</button>
              </div>
            </div>
            
            <!-- reCAPTCHA Container -->
            <div id="recaptcha-container" class="mt-3"></div>
          </div>
        </div>
      </div>
    </div>
  </div>

  <script src="https://www.gstatic.com/firebasejs/9.6.10/firebase-app-compat.js"></script>
  <script src="https://www.gstatic.com/firebasejs/9.6.10/firebase-auth-compat.js"></script>
  <script src="/js/firebase-config.js"></script>
  <script src="/js/login.js"></script>
</body>
</html>