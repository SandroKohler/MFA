<!-- views/setup-mfa.ejs -->
<!DOCTYPE html>
<html lang="de">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>MFA einrichten - Firebase MFA Demo</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/css/styles.css">
</head>
<body>
  <div class="container mt-5">
    <div class="row justify-content-center">
      <div class="col-md-6">
        <div class="card">
          <div class="card-header">
            <h3 class="text-center">Multi-Faktor-Authentifizierung einrichten</h3>
          </div>
          <div class="card-body">
            <div id="authSection">
              <p>Bitte bestätigen Sie Ihre Identität, indem Sie Ihr Passwort erneut eingeben:</p>
              <form id="reauthForm">
                <div class="mb-3">
                  <label for="password" class="form-label">Passwort:</label>
                  <input type="password" class="form-control" id="password" required>
                </div>
                <div class="d-grid">
                  <button type="submit" class="btn btn-primary">Authentifizieren</button>
                </div>
              </form>
            </div>
            
            <div id="setupSection" class="d-none">
              <p>Geben Sie Ihre Telefonnummer ein, um die Multi-Faktor-Authentifizierung einzurichten:</p>
              <form id="setupForm">
                <div class="mb-3">
                  <label for="phoneNumber" class="form-label">Telefonnummer (mit Ländervorwahl):</label>
                  <input type="tel" class="form-control" id="phoneNumber" placeholder="+49123456789" required>
                </div>
                <div class="mb-3">
                  <label for="displayName" class="form-label">Anzeigename für diesen Faktor:</label>
                  <input type="text" class="form-control" id="displayName" placeholder="Mein Handy" required>
                </div>
                <div class="d-grid">
                  <button type="submit" class="btn btn-primary">SMS-Code senden</button>
                </div>
              </form>
              
              <!-- reCAPTCHA Container -->
              <div id="recaptcha-container" class="mt-3"></div>
            </div>
            
            <div id="verificationSection" class="d-none">
              <p>Ein SMS-Code wurde an Ihre Telefonnummer gesendet. Bitte geben Sie diesen Code ein:</p>
              <form id="verificationForm">
                <div class="mb-3">
                  <label for="verificationCode" class="form-label">SMS-Code:</label>
                  <input type="text" class="form-control" id="verificationCode" required>
                </div>
                <div class="d-grid">
                  <button type="submit" class="btn btn-success">Bestätigen</button>
                </div>
              </form>
            </div>
            
            <div id="successSection" class="d-none">
              <div class="alert alert-success">
                <h4>MFA erfolgreich eingerichtet!</h4>
                <p>Sie haben die Multi-Faktor-Authentifizierung erfolgreich aktiviert.</p>
                <p>Bei der nächsten Anmeldung werden Sie aufgefordert, einen SMS-Code einzugeben.</p>
              </div>
              <div class="d-grid">
                <a href="/dashboard" class="btn btn-primary">Zurück zum Dashboard</a>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>

  <script src="https://www.gstatic.com/firebasejs/9.6.10/firebase-app-compat.js"></script>
  <script src="https://www.gstatic.com/firebasejs/9.6.10/firebase-auth-compat.js"></script>
  <script src="/js/firebase-config.js"></script>
  <script src="/js/setup-mfa.js"></script>
</body>
</html>