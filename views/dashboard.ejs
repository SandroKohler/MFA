<!-- views/dashboard.ejs -->
<!DOCTYPE html>
<html lang="de">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Dashboard - Firebase MFA Demo</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/css/styles.css">
</head>
<body>
  <div class="container mt-5">
    <div class="row justify-content-center">
      <div class="col-md-8">
        <div class="card">
          <div class="card-header d-flex justify-content-between align-items-center">
            <h3>Dashboard</h3>
            <button id="logoutBtn" class="btn btn-danger">Abmelden</button>
          </div>
          <div class="card-body">
            <h4 id="welcomeMessage">Willkommen!</h4>
            <p>Sie sind erfolgreich angemeldet.</p>
            
            <div id="userInfo" class="mt-4">
              <h5>Benutzerinformationen</h5>
              <p>E-Mail: <span id="userEmail"></span></p>
              <p>MFA aktiviert: <span id="mfaStatus"></span></p>
            </div>
            
            <div id="mfaSection" class="mt-4">
              <h5>Multi-Faktor-Authentifizierung</h5>
              <div id="mfaNotEnabled">
                <p>Sie haben die Multi-Faktor-Authentifizierung noch nicht aktiviert.</p>
                <button id="setupMfaBtn" class="btn btn-primary">MFA einrichten</button>
              </div>
              <div id="mfaEnabled" class="d-none">
                <p>Sie haben die Multi-Faktor-Authentifizierung aktiviert.</p>
                <p>Telefonnummer: <span id="mfaPhoneNumber"></span></p>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>

  <script src="https://www.gstatic.com/firebasejs/9.6.10/firebase-app-compat.js"></script>
  <script src="https://www.gstatic.com/firebasejs/9.6.10/firebase-auth-compat.js"></script>
  <script src="/js/firebase-config.js"></script>
  <script src="/js/dashboard.js"></script>
</body>
</html>